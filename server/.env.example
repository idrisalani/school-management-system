# Railway Environment Variables Template
NODE_ENV=production
PORT=3001
DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
CORS_ORIGIN=https://FRONTEND_SERVICE_NAME.railway.app

# Instructions:
# 1. DATABASE_URL will be auto-provided by Railway PostgreSQL
# 2. Replace FRONTEND_SERVICE_NAME with actual Railway frontend URL
# 3. Generate a strong JWT_SECRET (32+ characters)
# 4. Update these in Railway dashboard environment variables